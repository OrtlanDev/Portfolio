---
interface HeaderButtonProps {
    href: string;
    className: string;
}
const { href, className } = Astro.props as HeaderButtonProps;
---

<!-- # it's ok -->
<a
    class:list={[
        "flex items-center py-3 px-4 text-text-secondary hover:text-text-primary active:text-text-primary text-nowrap transition-colors",
        className,
    ]}
    href={"#" + href}><slot /></a
>
