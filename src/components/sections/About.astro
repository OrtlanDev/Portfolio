---
import Quotes from 'phosphor-astro/Quotes.astro';
import Anchor from '../ui/Anchor.astro';
import Title from '../ui/Title.astro';
const loop = Array.from({ length: 120 }, (i) => i);
---

<section class="relative flex items-center w-full h-160 overflow-hidden bg-background-light/75 justify-center">
  <!-- decoration -->
  <div class="absolute -top-5 w-max h-20 left-0 overflow-hidden px-2">
    {
      loop.map((i) => (
        <div class="w-10 h-10 inline-block bg-background -rotate-45 border-l-2 mr-3 border-background-dark/50">
          <div class="w-6 h-6  top-4 -left-9 inline-block absolute border-2 border-border/50 border-l-2" />
        </div>
      ))
    }
  </div>
  <div class="absolute -bottom-5 w-max h-20 -left-8 overflow-hidden z-20 rotate-180">
    {
      loop.map((i) => (
        <div class="w-10 h-10  inline-block bg-background -rotate-45 border-l-2 mr-3 border-background-dark/50">
          <div class="w-6 h-6 top-4 -left-9 inline-block absolute border-2 border-border/50 border-l-2" />
        </div>
      ))
    }
  </div>
  <div class="relative flex max-w-300 justify-center">
    <!-- info -->
    <div class="flex flex-col items-center gap-6 py-20 w-full">
      <!-- title -->
      <Title>
        <div class="mb-6 md:mb-0 md:mr-2 p-2 w-12 h-12 border-2 bg-border/20 border-border/55 rotate-45">
          <Quotes class="-rotate-45 w-full h-full text-primary" />
        </div>
        <h2>Sobre mí</h2>
      </Title>
      <div
        class="flex flex-col gap-2 md:gap-8 md:mt-4 font-light md:text-2xl text-xl text-center text-text-primary w-[90%] md:w-[70%] leading-relaxed md:leading-[3rem]"
      >
        <p>
          Soy diseñador <span class="font-sacramento text-3xl md:text-5xl text-primary">ux ui </span> y desarrollador <span
            class="font-sacramento text-3xl md:text-5xl text-primary">frontend</span
          > con más de 4 años de experiencia creando interfaces, y 1 año de experiencia laboral.
        </p>
        <p>
          Me especializo en construir experiencias <span class="font-sacramento text-3xl md:text-5xl text-primary"
            >intuitivas</span
          >, altamente <span class="font-sacramento text-3xl md:text-5xl text-primary">accesibles </span> y que funcionen
          tan bien como se ven.
        </p>
        <p>
          Trabajo con <span class="font-sacramento text-3xl md:text-5xl text-primary">Figma </span>
          <span class="italic text-text-secondary">(diseño web, móvil y maquetado)</span>, <span
            class="font-sacramento text-3xl md:text-5xl text-primary">Next.js</span
          >, React, TypeScript, TailwindCSS, y arquitecturas limpias.
        </p>
      </div>
    </div>
    <!-- <AboutImage1 /> -->
  </div>
  <Anchor id="about-section" className="-top-26" />
</section>
